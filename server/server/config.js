const config = {
    device_url: "localhost",
    device_port: 3001,
    dbuser: "example",
    dbpassword: "password",
    dbname: "api",
    dbcollection: "devices",
    db: "mongodb://mongo:27017/"
}

module.export = config;